{
  "name": "isaidooh",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "pnpm test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.90.0",
    "@cloudflare/kv-asset-handler": "^0.5.0"
  }
}
