{
  "name": "isaidooh",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply isaidooh-leaderboard --local",
    "db:migrate:remote": "wrangler d1 migrations apply isaidooh-leaderboard --remote",
    "test": "node --test",
    "check": "pnpm test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
